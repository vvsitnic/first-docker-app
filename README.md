# Docker

A bare-bones application with no meaning to get to know how to work with Docker

```bash
git clone https://github.com/vvsitnic/first-docker-app.git
cd first-docker-app
```

```bash
docker compose up
```
